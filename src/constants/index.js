export const emailRegexp = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
export const genderTypeList = ['man', 'woman'];
export const ACCESS_TOKEN_LIFETIME = 15 * 60 * 1000;
export const REFRESH_TOKEN_LIFETIME = 3 * 24 * 3600 * 1000;
